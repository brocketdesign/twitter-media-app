# Pinned to the versions this app is developed against locally.
flask==3.1.3
requests==2.32.5

# Invoked as `python -m gallery_dl` (see app.py), so it has to live in the
# same interpreter as the app — not just on PATH.
gallery-dl==1.32.8

# Production WSGI server. The Flask dev server in app.py's __main__ block is
# only used for local runs.
gunicorn==23.0.0
